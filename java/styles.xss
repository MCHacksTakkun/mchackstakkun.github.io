:root{
  --bg:#071029;
  --card:#0b1220;
  --accent:#f97316; /* Java向けアクセント色 */
  --muted:#94a3b8;
  --radius:12px;
  --glass: rgba(255,255,255,0.03);
  font-family: "Noto Sans JP", system-ui, -apple-system, "Segoe UI", Roboto, "Hiragino Kaku Gothic ProN", "Yu Gothic", sans-serif;
  color-scheme: dark;
}
*{box-sizing:border-box}
html,body{height:100%;margin:0;background:linear-gradient(180deg,#041022,var(--bg));color:#e6eef6}
.app{max-width:1100px;margin:28px auto;padding:18px}
.header{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:18px}
.brand{display:flex;gap:12px;align-items:center}
.logo{width:56px;height:56px;border-radius:10px;background:linear-gradient(135deg,var(--accent),#f59e0b);display:flex;align-items:center;justify-content:center;font-weight:700;color:#042027}
h1{margin:0;font-size:18px}
.subtitle{margin:0;color:var(--muted);font-size:13px}
.controls{display:flex;gap:8px;align-items:center}
input[type="search"]{padding:8px 10px;border-radius:10px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}
.btn{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--accent);padding:8px 12px;border-radius:10px;cursor:pointer}
.main{display:grid;grid-template-columns:1fr 380px;gap:18px;align-items:start}
.cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:14px}
.card{background:linear-gradient(180deg,var(--card), rgba(255,255,255,0.01));padding:14px;border-radius:12px;border:1px solid rgba(255,255,255,0.03);cursor:pointer;display:flex;flex-direction:column;gap:8px;transition:transform .15s,box-shadow .15s}
.card:focus,.card:hover{transform:translateY(-6px);box-shadow:0 12px 30px rgba(2,6,23,0.6);outline:none;border-color:rgba(249,115,22,0.12)}
.cardHeader{display:flex;gap:12px;align-items:center}
.thumb{width:56px;height:56;border-radius:8px;background:var(--glass);display:flex;align-items:center;justify-content:center;font-weight:700;color:var(--accent)}
.cardTitle{font-size:15px;margin:0}
.cardDesc{font-size:13px;color:var(--muted);margin:0}
.badges{display:flex;gap:6px;margin-top:8px}
.badge{font-size:12px;padding:6px 8px;border-radius:999px;background:rgba(255,255,255,0.02);color:var(--muted);border:1px solid rgba(255,255,255,0.02)}

.detail{background:linear-gradient(180deg,var(--card), rgba(255,255,255,0.01));padding:18px;border-radius:12px;border:1px solid rgba(255,255,255,0.03);position:relative;min-height:220px}
.close{position:absolute;top:10px;right:10px;background:transparent;border:0;color:var(--muted);font-size:20px;cursor:pointer}
.detailContent h2{margin:0 0 8px 0}
.detailContent p{color:var(--muted);margin:0 0 12px 0}
.links{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
.linkBtn{display:inline-flex;align-items:center;gap:8px;padding:8px 12px;border-radius:10px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:var(--accent);text-decoration:none}
.linkBtn.secondary{color:var(--muted);border-color:rgba(255,255,255,0.03)}
.youtubeEmbed{width:100%;aspect-ratio:16/9;border-radius:8px;overflow:hidden;background:#000;margin-top:12px}
.footer{text-align:center;margin-top:14px;color:var(--muted);font-size:13px}

/* レスポンシブ */
@media (max-width:980px){
  .main{grid-template-columns:1fr}
  .detail{order:2;margin-top:12px}
}
